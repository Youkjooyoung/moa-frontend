VITE_API_BASE_URL=http://localhost:8080/api

# Optional: only needed when running Vite with the local moa-ssl.p12 file.
# Keep the real passphrase in your shell environment or untracked .env file.
MOA_DEV_HTTPS_PASSPHRASE=
